# [260206_BOJ_BinSearch_S4_수_찾기]
> [문제 출처](https://www.acmicpc.net/problem/1920) <br/>
> **파일 네이밍 규칙**: `YYMMDD_플랫폼_유형_난이도_제목.md`
> - **플랫폼**: BOJ(백준), PGS(프로그래머스), SEA(삼성SW), LTC(리트코드)
> - **유형**: Impl(구현), BFS, DFS, Greedy, DP, Stack, Queue, Hash 등
> - **난이도**: Lv1, G5(골드5), S3(실버3) 등
> - **예시**: `251205_PGS_Impl_Lv1_유연근무제.md`

## 1. 🔍 분석 (10분)
- **제약 & 복잡도**:
   - N = 10^5 -> O(logN)
   - M = 10^5 -> O(M)
   - All -> O(MlogN)
   - 정수의 범위가 +- 2^31 -> hash table 만들지 말라는 의미 / 음수와 양수 모두 가짐
- **유형 & 자료구조**: Tree
- **핵심 로직 (Key Idea)**: 
> 최대한 빠르게 요소를 찾는 로직 필요 : binary Tree -> 어떻게 구현?
   1. [전처리] 
      - 입력받은 A 배열을 이진트리 형태로 바꿔야 함. 
      - 트리 리스트가 비어있으면 루트 노드로 추가 
         - 숫자를 하나씩 순회하며 (부모 노드가 i)
            - (루트) 노드보다 작으면 좌측노드로 이동 = 2i+1 에 저장
            - (루트) 노드보다 크면 우측노드로 이동 = 2i+2 에 저장
   2. [메인 로직] (예외/탈출 조건 포함)
      - M개의 입력받은 숫자를 이진트리에서 루트 경로부터 비교 
         - 노드의 값 보다 작으면 좌측 노드로, 크면 우측 노드로 이동하여 다시 비교 
         - 이동했을 떄 동일한 값이 있으면 1을 반환 
         - 더이상 내려갈 노드가 없으면 0을 반환 
   3. [최종 결과] (return 값)
      - M개의 수들이 각각 존재하는 지를 나타내는 1과 0을 각각 출력
- **체크 포인트**:
   - [ ] [참고 시트에서 선택]

---

### 🔄 피드백 반영 수정본
- **유형 & 자료구조**: BinSearch(이분탐색)
- **핵심 로직 (Key Idea)**: 
> N개의 데이터를 정렬한 뒤, 이분 탐색을 통해 M개의 데이터가 존재하는지 확인 (O((N+M)logN))
   1. [전처리] 
      - 입력받은 A 배열을 오름차순으로 정렬한다. (Binary Search의 필수 전제)
   2. [메인 로직]
      - M개의 타겟 숫자들을 순회하며 각각 이분 탐색 수행
      - `start = 0`, `end = N-1`로 설정하고 `while start <= end:` 동안 반복
         - `mid = (start + end) // 2`
         - `A[mid]` == `target`: 찾았음 (1 반환)
         - `A[mid]` > `target`: 타겟이 왼쪽에 있음 -> `end = mid - 1`
         - `A[mid]` < `target`: 타겟이 오른쪽에 있음 -> `start = mid + 1`
      - 반복문이 끝날 때까지 못 찾으면 0 반환
   3. [최종 결과] (return 값)
      - 각 타겟 숫자에 대한 탐색 결과(1 또는 0)를 출력

---

## 2. 📚 학습 (5분)
> 이 문제를 통해 새로 알게 된 점이나 다시 확인한 개념을 정리합니다.

**① 오늘의 알고리즘/개념**
> "찾는 문제는 Set 아니면 이분탐색이다" 
- **이분 탐색 (Binary Search)**:
  - **비유**: **Up/Down 게임**. 반씩 쪼개며 찾음.
  - **특징**: 데이터가 반드시 **정렬**되어 있어야 함. 시간복잡도 $O(\log N)$.
- **Set (Hash Table)**:
  - **비유**: **도서관 사서**. 책 제목만 대면 위치를 바로 집어줌.
  - **원리**: 내부적으로 **해시 함수(Hash Function)**를 사용. 
    - 값(`1920`)을 넣으면 고유한 주소(`0x1234`)를 즉시 계산해냄.
    - 따라서 정렬이 안 되어 있어도, 데이터가 100만 개여도 **딱 한 번($O(1)$)** 만에 찾음.

**② Python Code Snippet (패턴 저장)**
> 이분 탐색 표준 구현 (While Loop) & 라이브러리 활용
```python
# 1. 표준 이분 탐색 구현 (While Loop)
def binary_search(arr, target):
    start, end = 0, len(arr) - 1
    while start <= end:
        mid = (start + end) // 2
        if arr[mid] == target:
            return 1        # 찾음
        elif arr[mid] > target:
            end = mid - 1   # 타겟이 왼쪽
        else:
            start = mid + 1 # 타겟이 오른쪽
    return 0                # 못 찾음

# 2. Python 내장 라이브러리 활용 (bisect)
from bisect import bisect_left, bisect_right
# bisect_right - bisect_left > 0 이면 존재
idx = bisect_left(arr, target)
if idx < len(arr) and arr[idx] == target:
    print(1)
```

---

<!-- 💡 사용 팁

## 💡 참고 시트
> 유형, 자료구조, 복잡도, 체크 포인트를 정리합니다.

1. 🎯 목표 복잡도 가이드 (N의 크기 ➜ 시간 복잡도)
   - N ≤ 20     ➜ O(2^N)  : 백트래킹, 재귀, 완전탐색
   - N ≤ 1,000  ➜ O(N^2)  : 2중 For문, DP, 2차원 배열
   - N ≤ 10만   ➜ O(NlogN): 정렬, 힙, 이분탐색, 다익스트라, 유니온파인드
   - N ≤ 100만  ➜ O(N)    : 투 포인터, 그리디, DP, 스택/큐, 해시
-------------------------------------------------------
2. 🧩 알고리즘 유형 (파일명 약어)
   - [기본] Impl(구현), Sim(시뮬레이션), BF(브루트포스, 완전탐색)
   - [탐색] BFS(너비우선), DFS(깊이우선), Back(백트래킹), BinSearch(이분탐색)
   - [최적화] Greedy(그리디), DP(동적계획법)
   - [그래프] Dijkstra(다익스트라), Floyd(플로이드워셜), Topo(위상정렬), MST
   - [기타] TwoPtr(투 포인터), Sliding(슬라이딩 윈도우), Prime(소수판별)
-------------------------------------------------------
3. 📦 자료구조 선택
   - [선형] List(배열), Stack(스택), Queue(큐), Deque(덱)
   - [비선형] Graph(인접리스트/행렬), Tree(트리), Heap(우선순위 큐)
   - [검색/집합] Set(집합), Dictionary(해시맵)
-------------------------------------------------------
4. ⚠️ 필수 체크 포인트 (Edge Case)
   - [ ] N=1 또는 0일 때 (최소 입력)
   - [ ] 데이터가 없는 경우 (Empty)
   - [ ] 모든 값이 동일한 경우
   - [ ] 정답이 음수이거나 0일 가능성
   - [ ] 값의 변화가 특정 범위를 벗어나면 안 되는가? (Max/Min Check)
   - [ ] 재귀 깊이 제한 (sys.setrecursionlimit)
   - [ ] 리스트 복사 주의 (얕은 복사 vs 깊은 복사)
-------------------------------------------------------

-->