# [260206_BOJ_BinSearch_S4_숫자찾기_2]
> [문제 출처](https://www.acmicpc.net/problem/10816) <br/>

## 1. 🔍 분석 (10분)
- **제약 & 복잡도**:
   - N = 500,000 -> O(N)
- **유형 & 자료구조**: BinSearch
- **핵심 로직 (Key Idea)**: 
   1. [전처리] 
   - 상근이의 카드 N개를 입력받고 Counter 객체를 만든다. 
   - 카드를 중복 제거 후 오름차순으로 정렬한다. 
   - 확인을 할 범위 start와 end 를 시작과 끝값으로 정의한다. 
   - 정답을 담을 리스트를 정의한다. ; result 
   2. [메인 로직] (예외/탈출 조건 포함)
   - while 루프를 사용하여 start < end 인 상황에서 작동하도록 한다. 
      - 중앙값인 mid = start+end 를 절반으로 나눈 값을 정의한다. 
      - 찾는 값이 mid보다 크면: 
         - start를 mid + 1로 바꾼다. 
      - 찾는 값이 mid보다 작으면: 
         - end를 mid -1로 바꾼다. 
      - 찾는 값이 mid와 같으면: 
         - Counter 객체에서 해당 값의 개수를 result에 넣는다. 
   - 없으면 0을 result에 넣는다. 

   3. [최종 결과] (return 값)
   - 찾는 카드의 개수를 담은 result를 공백으로 구분하여 출력한다. 

   > 피드백 : Couter 객체를 만듦으로서 이미 hash table을 가진 것인데 이진분류를 쓸 필요없음

- **체크 포인트**:
   - [x] N=1 또는 0일 때 (최소 입력)
   - [x] 데이터가 없는 경우 (Empty)
   - [x] 모든 값이 동일한 경우
   - [x] 정답이 음수이거나 0일 가능성
   - [x] 값의 변화가 특정 범위를 벗어나면 안 되는가? (Max/Min Check)

---

## 2. 📚 학습 (5분)
> 이 문제를 통해 새로 알게 된 점이나 다시 확인한 개념을 정리합니다.

**① 오늘의 알고리즘/개념**
> **Bisect 라이브러리 & Lower/Upper Bound**
- **시간 복잡도**: 수행 시간 **O(log N)** (전제: 정렬된 리스트)
- **개념 비교**:
  - **`bisect_left(arr, x)`**: `x`를 정렬 순서를 유지하며 삽입할 때 **가장 왼쪽** 위치. (즉, `x` **이상**인 첫 위치)
  - **`bisect_right(arr, x)`**: `x`를 정렬 순서를 유지하며 삽입할 때 **가장 오른쪽** 위치. (즉, `x` **초과**인 첫 위치)
- **[예시]** `arr = [1, 2, 4, 4, 8]`, `target = 4`
  - `bisect_left`: 인덱스 **2** (첫 번째 `4` 앞)
  - `bisect_right`: 인덱스 **4** (두 번째 `4` 뒤, 즉 `8` 앞)
  - **개수**: `right(4) - left(2) = 2개`
- **활용**: 특정 값의 개수 구하기, 존재 유무 확인(`left` 인덱스 값 확인)

**② Python Code Snippet (패턴 저장)**
> `bisect` 활용: 개수 구하기 & 존재 유무 확인
```python
from bisect import bisect_left, bisect_right

# 1. 특정 구간(또는 값)의 개수 구하기
def count_by_range(array, left_value, right_value):
    right_index = bisect_right(array, right_value)
    left_index = bisect_left(array, left_value)
    return right_index - left_index

# 2. 특정 값의 존재 유무 확인 (Contains)
def contains(array, target):
    idx = bisect_left(array, target)
    # 인덱스가 범위 내에 있고, 그 위치의 값이 target과 같아야 함
    if idx < len(array) and array[idx] == target:
        return True
    return False

# 사용 예시
# data.sort()
# cnt = count_by_range(data, 4, 4)  # 4의 개수
# has_four = contains(data, 4)      # 4의 존재 여부 (True/False)
```

---

<!-- 💡 사용 팁

## 💡 참고 시트
> 유형, 자료구조, 복잡도, 체크 포인트를 정리합니다.

1. 🎯 목표 복잡도 가이드 (N의 크기 ➜ 시간 복잡도)
   - N ≤ 20     ➜ O(2^N)  : 백트래킹, 재귀, 완전탐색
   - N ≤ 1,000  ➜ O(N^2)  : 2중 For문, DP, 2차원 배열
   - N ≤ 10만   ➜ O(NlogN): 정렬, 힙, 이분탐색, 다익스트라, 유니온파인드
   - N ≤ 100만  ➜ O(N)    : 투 포인터, 그리디, DP, 스택/큐, 해시
-------------------------------------------------------
2. 🧩 알고리즘 유형 (파일명 약어)
   - [기본] Impl(구현), Sim(시뮬레이션), BF(브루트포스, 완전탐색)
   - [탐색] BFS(너비우선), DFS(깊이우선), Back(백트래킹), BinSearch(이분탐색)
   - [최적화] Greedy(그리디), DP(동적계획법)
   - [그래프] Dijkstra(다익스트라), Floyd(플로이드워셜), Topo(위상정렬), MST
   - [기타] TwoPtr(투 포인터), Sliding(슬라이딩 윈도우), Prime(소수판별)
-------------------------------------------------------
3. 📦 자료구조 선택
   - [선형] List(배열), Stack(스택), Queue(큐), Deque(덱)
   - [비선형] Graph(인접리스트/행렬), Tree(트리), Heap(우선순위 큐)
   - [검색/집합] Set(집합), Dictionary(해시맵)
-------------------------------------------------------
4. ⚠️ 필수 체크 포인트 (Edge Case)
   - [ ] N=1 또는 0일 때 (최소 입력)
   - [ ] 데이터가 없는 경우 (Empty)
   - [ ] 모든 값이 동일한 경우
   - [ ] 정답이 음수이거나 0일 가능성
   - [ ] 값의 변화가 특정 범위를 벗어나면 안 되는가? (Max/Min Check)
   - [ ] 재귀 깊이 제한 (sys.setrecursionlimit)
   - [ ] 리스트 복사 주의 (얕은 복사 vs 깊은 복사)
-------------------------------------------------------

-->