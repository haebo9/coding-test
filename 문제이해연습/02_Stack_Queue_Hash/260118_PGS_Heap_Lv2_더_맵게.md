# [260118_PGS_Heap_Lv2_더_맵게]
> [문제 출처](https://school.programmers.co.kr/learn/courses/30/lessons/42626) <br/>

## 1. 🔍 분석 (10분)
- **제약 & 복잡도**:
   - N = 10^6 -> O(N) `# 수정 -> (N logN)`
   - 원소의 값 = 10^6 
   - `(수정 이유) => # 힙 생성시 O(N) * Heappush.Heappop O(logN)`
- **유형 & 자료구조**: Heap
- **핵심 로직 (Key Idea)**: 
   1. [전처리] 
      - scoville 배열을 힙 객체로 선언한다. : heapq
      - 섞었을 때의 스코빌 지수값을 계산하는 함수 정의 def scoville_sum
      - 섞은 횟수 초기화 변수 선언 ; mix_count = 0 

   2. [메인 로직] (예외/탈출 조건 포함)
      - while 무한 루프: 
         - 가장 작은 요소(heap[0])가 K를 넘는 지 확인 (제일 작은게 > K)
            - 넘었으면 지금까지 섞은 횟수 리턴 
         - haep 요소의 개수가 2개 이상인지 확인 
            - 아니면 -1 리턴 
         - 힙 객체에서 우선순위가 높은 두개의 값을 뽑는다. ; heappop()
            - scoville_sum 함수에 넣어 값을 얻는다. 
            - 다시 객체에 넣는다. ; heappush() 
         - 섞은 횟수 + 1 

   3. [최종 결과] (return 값)
      - 반복문 중간에서의 섞은 횟수 값 또는 -1 

- **체크 포인트**:
   - [x] N=1 또는 0일 때 (최소 입력)
   - [x] 데이터가 없는 경우 (Empty)
   - [x] 모든 값이 동일한 경우
   - [x] 정답이 음수이거나 0일 가능성
   - [x] 값의 변화가 특정 범위를 벗어나면 안 되는가? (Max/Min Check)
   - [x] 재귀 깊이 제한 (sys.setrecursionlimit)
   - [x] 리스트 복사 주의 (얕은 복사 vs 깊은 복사)

---

## 2. 📚 학습 (5분)
> 이 문제를 통해 새로 알게 된 점이나 다시 확인한 개념을 정리합니다.

**① 오늘의 알고리즘/개념**
> **Heap (Priority Queue)**: 최댓값/최솟값을 `O(log N)`에 찾아내는 효율적인 자료구조
- **성질**: 파이썬의 `heapq`는 기본적으로 **최소 힙(Min Heap)**이다. (부모 노드 <= 자식 노드)
- **복잡도**: 
  - 삽입(`heappush`) / 삭제(`heappop`): `O(log N)`
  - 리스트를 힙으로 변환(`heapify`): `O(N)` (하나씩 push 하는 것보다 빠름)
- **활용**: 데이터의 삽입/삭제가 빈번하면서 항상 정렬된 상태(혹은 극값)가 필요할 때 사용. 매번 `sort()`를 쓰면 `O(N log N)`이 걸리지만, 힙은 `O(log N)`으로 해결 가능.

**② Python Code Snippet (패턴 저장)**
> **Heapq 필수 패턴 (Min Heap & Max Heap)**
```python
import heapq

# 1. 최소 힙 (Min Heap) - 기본
data = [1, 5, 3]
heapq.heapify(data)       # List -> Heap 변환: O(N)
min_val = heapq.heappop(data) # 최솟값 추출: O(log N)
heapq.heappush(data, 2)   # 원소 추가: O(log N)

# 2. 최대 힙 (Max Heap) - 응용
# 부호(-)를 반대로 뒤집어서 저장하고, 꺼낼 때 다시 붙임
max_heap = []
heapq.heappush(max_heap, -10) 
heapq.heappush(max_heap, -5)
max_val = -heapq.heappop(max_heap) # -(-10) = 10 (최댓값)
```

---

<!-- 💡 사용 팁

## 💡 참고 시트
> 유형, 자료구조, 복잡도, 체크 포인트를 정리합니다.

1. 🎯 목표 복잡도 가이드 (N의 크기 ➜ 시간 복잡도)
   - N ≤ 20     ➜ O(2^N)  : 백트래킹, 재귀, 완전탐색
   - N ≤ 1,000  ➜ O(N^2)  : 2중 For문, DP, 2차원 배열
   - N ≤ 10만   ➜ O(NlogN): 정렬, 힙, 이분탐색, 다익스트라, 유니온파인드
   - N ≤ 100만  ➜ O(N)    : 투 포인터, 그리디, DP, 스택/큐, 해시
-------------------------------------------------------
2. 🧩 알고리즘 유형 (파일명 약어)
   - [기본] Impl(구현), Sim(시뮬레이션), BF(브루트포스, 완전탐색)
   - [탐색] BFS(너비우선), DFS(깊이우선), Back(백트래킹), BinSearch(이분탐색)
   - [최적화] Greedy(그리디), DP(동적계획법)
   - [그래프] Dijkstra(다익스트라), Floyd(플로이드워셜), Topo(위상정렬), MST
   - [기타] TwoPtr(투 포인터), Sliding(슬라이딩 윈도우), Prime(소수판별)
-------------------------------------------------------
3. 📦 자료구조 선택
   - [선형] List(배열), Stack(스택), Queue(큐), Deque(덱)
   - [비선형] Graph(인접리스트/행렬), Tree(트리), Heap(우선순위 큐)
   - [검색/집합] Set(집합), Dictionary(해시맵)
-------------------------------------------------------
4. ⚠️ 필수 체크 포인트 (Edge Case)
   - [ ] N=1 또는 0일 때 (최소 입력)
   - [ ] 데이터가 없는 경우 (Empty)
   - [ ] 모든 값이 동일한 경우
   - [ ] 정답이 음수이거나 0일 가능성
   - [ ] 값의 변화가 특정 범위를 벗어나면 안 되는가? (Max/Min Check)
   - [ ] 재귀 깊이 제한 (sys.setrecursionlimit)
   - [ ] 리스트 복사 주의 (얕은 복사 vs 깊은 복사)
-------------------------------------------------------

-->