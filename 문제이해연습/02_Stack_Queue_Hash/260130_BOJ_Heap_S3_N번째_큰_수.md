# 260130_BOJ_Heap_S3_N번째_큰_수
> [문제 출처](https://www.acmicpc.net/problem/2075) <br/>

## 1. 🔍 분석 (10분)
- **제약 & 복잡도**:
   - N = 1500 -> N^2 = 2.25 * 10^6
   - **⚠️ 중요**: 메모리 제한이 12MB로 매우 빡빡함.
   - Python 정수 1개(28byte) * 225만개 ≈ 63MB -> **모든 수를 저장하면 메모리 초과 발생**.
   - 따라서, 데이터를 다 저장하지 않고 **필요한 상위 N개만 유지**해야 함.
- **유형 & 자료구조**: heapq (Min Heap)
- **핵심 로직 (Key Idea)**: 
   > N번째 큰 수를 구해야 하므로, 상위 N개만 들고 다니면 된다.
   > 크기 N인 최소 힙(Min Heap)을 유지하면, 루트 노드(`heap[0]`)가 항상 "현재까지 본 수들 중 N번째로 큰 수"가 된다.
   1. [초기화] 
      - 첫 번째 줄의 숫자 N개로 최소 힙을 채운다.
   2. [메인 로직] (한 줄씩 읽으며 갱신)
      - 이후 줄의 숫자들을 하나씩 확인한다.
      - 들어온 수 `x`가 힙의 최솟값(`heap[0]`)보다 크다면?
        - `heapq.heappushpop(heap, x)`: 최솟값을 버리고 `x`를 넣어서 다시 정렬.
        - (작거나 같으면 무시해도 됨, 어차피 N위 밖임)
   3. [최종 결과] (return 값)
      - 모든 입력을 처리한 후 `heap[0]` 출력.
- **체크 포인트**:
   - [x] 메모리 초과 주의 (전체 리스트 저장 금지)
   - [ ] 값의 범위 확인 (-10억 ~ 10억)

---

## 2. 📚 학습 (5분)
> 이 문제를 통해 새로 알게 된 점이나 다시 확인한 개념을 정리합니다.

**① 오늘의 알고리즘/개념**
**① 오늘의 알고리즘/개념**
> "N번째 큰 수" = "상위 N개 집합 중 가장 작은 수" (메모리 최적화의 핵심)
- **메모리 제한 극복**: $N=1500$일 때 $N^2 \approx 225$만 개. Python 정수(28byte)로 모두 저장 시 12MB 초과.
- **최소 힙(Min Heap) 전략**: 크기가 **N**인 힙을 유지. 새로운 수가 힙의 최솟값(Root, 즉 현재의 N등)보다 클 때만 교체(`pushpop`).
  - 힙에는 항상 **"현재까지 본 수들 중 가장 큰 N개"**만 남는다.
  - 이 중 가장 작은 값(`heap[0]`)이 바로 **N번째 큰 수**가 된다.
- **관점 전환**: 전체 $N^2$개 중 하위 $N^2 - N$개를 버리고, 살아남은 상위 그룹(N개)의 꼴찌를 찾는 과정.

**② Python Code Snippet (패턴 저장)**
> 크기 N으로 고정된 Min Heap 패턴
```python
import heapq

# 초기 N개 채우기
hq = [] # 크기 N 유지

# 스트림 방식 처리 (메모리 절약)
for num in stream_numbers:
    if len(hq) < N:
        heapq.heappush(hq, num)
    elif num > hq[0]: # 힙의 최솟값(커트라인)보다 크면 교체
        heapq.heappushpop(hq, num)

print(hq[0]) # N번째 큰 수
```

---

<!-- 💡 사용 팁

## 💡 참고 시트
> 유형, 자료구조, 복잡도, 체크 포인트를 정리합니다.

1. 🎯 목표 복잡도 가이드 (N의 크기 ➜ 시간 복잡도)
   - N ≤ 20     ➜ O(2^N)  : 백트래킹, 재귀, 완전탐색
   - N ≤ 1,000  ➜ O(N^2)  : 2중 For문, DP, 2차원 배열
   - N ≤ 10만   ➜ O(NlogN): 정렬, 힙, 이분탐색, 다익스트라, 유니온파인드
   - N ≤ 100만  ➜ O(N)    : 투 포인터, 그리디, DP, 스택/큐, 해시
-------------------------------------------------------
2. 🧩 알고리즘 유형 (파일명 약어)
   - [기본] Impl(구현), Sim(시뮬레이션), BF(브루트포스, 완전탐색)
   - [탐색] BFS(너비우선), DFS(깊이우선), Back(백트래킹), BinSearch(이분탐색)
   - [최적화] Greedy(그리디), DP(동적계획법)
   - [그래프] Dijkstra(다익스트라), Floyd(플로이드워셜), Topo(위상정렬), MST
   - [기타] TwoPtr(투 포인터), Sliding(슬라이딩 윈도우), Prime(소수판별)
-------------------------------------------------------
3. 📦 자료구조 선택
   - [선형] List(배열), Stack(스택), Queue(큐), Deque(덱)
   - [비선형] Graph(인접리스트/행렬), Tree(트리), Heap(우선순위 큐)
   - [검색/집합] Set(집합), Dictionary(해시맵)
-------------------------------------------------------
4. ⚠️ 필수 체크 포인트 (Edge Case)
   - [ ] N=1 또는 0일 때 (최소 입력)
   - [ ] 데이터가 없는 경우 (Empty)
   - [ ] 모든 값이 동일한 경우
   - [ ] 정답이 음수이거나 0일 가능성
   - [ ] 값의 변화가 특정 범위를 벗어나면 안 되는가? (Max/Min Check)
   - [ ] 재귀 깊이 제한 (sys.setrecursionlimit)
   - [ ] 리스트 복사 주의 (얕은 복사 vs 깊은 복사)
-------------------------------------------------------

-->